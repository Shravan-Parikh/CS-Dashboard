# Copy to .env.local. Base URL of the backend API.
NEXT_PUBLIC_API_BASE=http://localhost:4000/api

# Set true only if you want open self-service sign-up (pilot: keep false).
NEXT_PUBLIC_ALLOW_SIGNUP=false
